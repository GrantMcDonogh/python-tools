[project]
name = "pdf-extractor"
version = "0.1.0"
description = "Extract text from PDF documents and save to text files"
requires-python = ">=3.8"
dependencies = [
    "pymupdf>=1.23.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
